# This file was generated with `cornucopia`. Do not modify
[package]
name = "codegen"
version = "0.9.0"
edition = "2021"

[features]
default = ["deadpool"]
deadpool = ["dep:deadpool-postgres"]

[dependencies]
## Core dependencies
# Postgres types
postgres-types = { version = "0.2.6", features = ["derive"] }
# Postgres interaction
postgres-protocol = "0.6.6"
# Iterator utils required for working with `postgres_protocol::types::ArrayValues`
fallible-iterator = "0.2.0"

## Types dependencies
# JSON or JSONB
serde_json = { version = "1.0.113", features = ["raw_value"] }
serde = { version = "1.0.197", features = ["derive"] }
# TIME, DATE, TIMESTAMP or TIMESTAMPZ
time = "0.3.34"
# UUID
uuid = "1.8.0"
# MAC ADDRESS
eui48 = "1.1.0"
# DECIMAL
rust_decimal = { version = "1.29.1", features = ["db-postgres"] } 

## Sync client dependencies
# Postgres sync client
postgres = { version = "0.19.7", features = ["with-serde_json-1","with-time-0_3","with-uuid-1","with-eui48-1",] }

## Async client dependencies
# Postgres async client
tokio-postgres = { version = "0.7.10", features = ["with-serde_json-1","with-time-0_3","with-uuid-1","with-eui48-1",] }
# Async utils
async-trait = "0.1.78"
futures = "0.3.30"

## Async features dependencies
# Async connection pooling
deadpool-postgres = { version = "0.12.1", optional = true }
